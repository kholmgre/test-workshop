import { assert } from 'chai';

/**
 * This is a basic walkthrough of how to build a unit test. We will be writing the code under test in this file and then use the templates below to 
 * create our tests.
 */

// In this exercise, create a function that takes a number as input and returns that number + 2
describe('Exercise 1', function () {
    it('should assert the code under test is working', function () {
        // Arrange
        
        // Write your function here
        // Also declare a variable of what you expect the result to be
        // 

        // Act
        // Invoke your new method here and store the result in a variable

        // Assert
        // Assert that the result equals the expected result
    });
});
















// Hindsight

// Exercise 1
// // Arrange
// const func = (arg) => { return arg + 2 };
// const input = 2;
// const expected = input + 2;

// // Act
// const result = func(input);

// // Assert
// assert.equal(expected, result);